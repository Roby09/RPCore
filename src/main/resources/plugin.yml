name: ${project.name}
main: ${project.groupId}.RPCore
description: ${project.description}
version: ${project.version}
author: roby08
depend: [PlaceholderAPI]
api-version: 1.18
commands:
  card:
    description: Display character information.
  character:
    description: Change character information.
  roll:
    description: Roll a random number.
  shout:
    description: Shout a message.
  whisper:
    description: Whisper a message.
  quiet:
    description: Send a quiet message.
  ooc:
    description: Send a ooc message.
  looc:
    description: Send a local ooc message.
  me:
    description: Send a local emote message.
  setrole:
    description: Sets the role of a player

